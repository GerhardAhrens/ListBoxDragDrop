<Window
    x:Class="ListBoxDragDrop.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ListBoxDragDrop"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    Title="{Binding Path=WindowTitel, FallbackValue=WindowTitel}"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Window.Icon>
        <BitmapImage UriSource="pack://application:,,,/Resources/Picture/ApplicationIcon.png" />
    </Window.Icon>

    <Window.Resources>
        <sys:String x:Key="ampersand">&amp;</sys:String>

    </Window.Resources>

    <Grid x:Name="gridMain">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.ColumnSpan="3" FontSize="14">
            <Run Text="ListBox mit" />
            <Run Text="Drag" />
            <Run Text="{StaticResource ResourceKey=ampersand}" />
            <Run Text="Drop" />
        </TextBlock>

        <Rectangle
            Grid.Row="0"
            Grid.ColumnSpan="3"
            Height="1"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Bottom"
            Fill="Blue" />

        <ListBox
            x:Name="LbWorkItem"
            Grid.Row="1"
            Grid.Column="0"
            Margin="5,0,0,5"
            VerticalAlignment="Stretch"
            AllowDrop="True" />

        <ListBox
            x:Name="LbInArbeit"
            Grid.Row="1"
            Grid.Column="1"
            Margin="0,0,0,5"
            VerticalAlignment="Stretch"
            AllowDrop="True" />

        <ListBox
            x:Name="LbFertig"
            Grid.Row="1"
            Grid.Column="2"
            Margin="0,0,5,5"
            VerticalAlignment="Stretch"
            AllowDrop="True" />
    </Grid>
</Window>
